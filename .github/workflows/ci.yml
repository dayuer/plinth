name: ci
on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - name: gofmt
        run: test -z "$(gofmt -l .)" || (gofmt -l . && exit 1)
      - name: vet
        run: go vet ./...
      - name: test
        run: go test ./... -race -count=1
  integration:
    runs-on: ubuntu-latest # Docker preinstalled for testcontainers
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - name: test (integration)
        run: go test -tags integration ./... -count=1 -timeout 10m
      - name: fuzz smoke
        run: go test ./internal/sqlscan/ -fuzz FuzzAnalyzeRewrite -fuzztime 30s
